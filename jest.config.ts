import type { Config } from 'jest';

const config: Config = {
    projects: [
        "jest-js.config.ts",
        "jest-solid.config.ts",
        "jest-react.config.ts"
    ]
};

export default config;